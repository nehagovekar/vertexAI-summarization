# Core frontend and API interaction
streamlit
requests
openai

# FastAPI backend + clean version pinning for stability
fastapi==0.95.2
pydantic==1.10.13

# Optional (for local dev only, safe to include)
uvicorn==0.24.0
python-dotenv
